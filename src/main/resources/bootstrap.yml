uri: ${vcap.services.configserver.credentials.uri}
---
spring:
  application:
    name: roster
  cloud:
    config:
      uri: ${uri}

management:
  security:
    enabled: false

security:
  basic:
    enabled: false
